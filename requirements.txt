# Core runtime
open_clip_torch>=2.24.0
torch>=2.0.0
psycopg[binary]>=3.1.0
Pillow>=10.0.0
numpy>=1.26.0
PyYAML>=6.0.0

# Phase 2 — face detection + recognition (InsightFace buffalo_l via ONNX)
# onnxruntime-gpu is installed instead on the PC for CUDA acceleration — see README.
insightface>=0.7.3
onnxruntime>=1.16.0
opencv-python-headless>=4.8.0

# Phase 2 — face clustering (Iter 2)
scikit-learn>=1.4.0

# Development / testing
pytest>=8.0.0
